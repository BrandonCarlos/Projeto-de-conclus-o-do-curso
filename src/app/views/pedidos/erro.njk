{% extends "layout.njk" %}
{% import "parts/card.njk" as cards %}

{% block content %}

  {%call cards.render('', 'pedido-erro container')%}
  <div class="erro-lottie"></div>
  <h2>Desculpe</h2>
  <p>
    Não podemos processar seu pedido
  </p>
  <a href="#" onclick="history.back()">Tente novamente</a>

  {%endcall%}

  <script src="/lottie.min.js"></script>
  <script>
    const element = document.querySelector('.erro-lottie')
    lottie.loadAnimation({
      container: element, // the dom element that will contain the animation
      renderer: 'svg',
      loop: true,
      autoplay: true,
      path: '/35174-error-animation.json' // the path to the animation json
    }).setSpeed(1)
  </script>

{% endblock content %}