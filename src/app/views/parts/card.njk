{% macro render(titulo, classes)%}
  <section class="card {{classes}}">

    <div class="card-header">
      <h3>
        {{titulo}}
      </h3>
    </div>

    <div class="card-content">
      {{ caller() }}
    </div>
  </section>
  {%endmacro%}